# Руководство по проверке токена eBay

**Дата:** 2025-01-XX  
**Проблема:** Токен не авторизован - `Connected as: None`

---

## 🚨 СТАТУС: КРИТИЧЕСКАЯ ОШИБКА – ТОКЕН НЕ АВТОРИЗОВАН

---

## 📋 Анализ логов:

- ❌ **Все API:** `Connected as: None (eBay UserID: None)` → токен не валиден или не передан
- ❌ **Orders:** 400 → `Invalid filter name: 'lastModifiedDate'` – но это следствие, а не причина
- ❌ **Transactions:** 404 – тоже следствие невалидного токена

---

## 🔍 ГЛАВНАЯ ПРОБЛЕМА: ТОКЕН НЕ РАБОТАЕТ

### Почему:

1. **Если токен невалиден, eBay возвращает 400/404/200 с пустым ответом** – как будто всё нормально, но без данных.
2. **`Connected as: None`** – это признак того, что Identity API не смог определить пользователя → токен не действителен.

---

## 🛠️ ИНСТРУКЦИЯ ДЛЯ ИСПРАВЛЕНИЯ:

### 1. Проверьте токен вручную:

```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     "https://api.ebay.com/identity/v1/oauth2/userinfo"
```

**Ожидаемые результаты:**
- ✅ Если **401/403/null** → токен невалиден
- ✅ Если **JSON с user_id** → токен валиден, проблема в коде

**Где взять токен:**
- Проверить в базе данных (таблица users, поле ebay_access_token)
- Или получить из логов backend (если логируется)

---

### 2. Если токен валиден – проверьте, как он передается в запросах:

- ✅ Убедитесь, что заголовок `Authorization: Bearer <token>` есть во всех запросах
- ✅ Проверьте, что токен не обрезан и не содержит пробелов
- ✅ Проверьте, что токен правильно извлекается из базы данных

**Проверка в коде:**
- Посмотреть как токен получается в `sync_all_orders`, `sync_all_transactions` и т.д.
- Убедиться что `access_token` не None и не пустой

---

### 3. Если токен невалиден – получите новый:

1. **Переподключитесь к eBay:**
   - Откройте приложение
   - Перейдите в "eBay Connection"
   - Нажмите "Disconnect from eBay"
   - Нажмите "Connect to eBay" снова
   - Пройдите OAuth flow

2. **Убедитесь, что scope включают:**
   - `sell.fulfillment` (для Orders)
   - `sell.finances` (для Transactions)
   - `sell.inventory` (для Inventory/Offers)
   - `trading` (для Messages)
   - Базовый scope для Identity API

3. **Проверьте в eBay Developer Portal:**
   - Убедитесь что все нужные scope активированы для вашего приложения

---

### 4. После получения нового токена:

- ✅ Запустите синхронизацию снова
- ✅ Проверьте логи - должно появиться `Connected as: XXXXX`
- ✅ Если все еще None - проверьте логи backend с детальным логированием

---

## 🎯 ЦЕЛЬ:

**Получить `Connected as: ХХХХХ` в логах** – доказать, что токен работает.

---

## 📝 Дополнительная диагностика:

### Проверка токена в коде:

Можно добавить endpoint для проверки токена:

```python
@router.get("/ebay/test-token")
async def test_token(user_id: str = Depends(get_current_user_id)):
    ebay_service = EbayService()
    user = db.get_user(user_id)
    if not user or not user.ebay_access_token:
        return {"error": "No token found"}
    
    identity = await ebay_service.get_user_identity(user.ebay_access_token)
    return {
        "token_exists": bool(user.ebay_access_token),
        "token_length": len(user.ebay_access_token) if user.ebay_access_token else 0,
        "identity": identity
    }
```

### Проверка scope токена:

Токен содержит информацию о scope в JWT payload. Можно декодировать и проверить:

```python
import jwt
# Декодировать токен (без проверки подписи, только для чтения)
decoded = jwt.decode(token, options={"verify_signature": False})
scopes = decoded.get("scope", [])
print(f"Token scopes: {scopes}")
```

---

## 🔧 Что уже сделано:

1. ✅ Добавлено детальное логирование Identity API
2. ✅ Исправлен endpoint на `/identity/v1/oauth2/userinfo`
3. ✅ Добавлена поддержка обоих форматов (`user_id` и `userId`)

---

## 📌 Следующие шаги:

1. **Проверить токен вручную** через curl
2. **Если токен невалиден** - переподключиться к eBay
3. **Если токен валиден** - проверить как он передается в запросах
4. **После исправления** - запустить синхронизацию и проверить логи


